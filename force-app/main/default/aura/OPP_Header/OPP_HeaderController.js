({
    init : function(component, event, helper) {
        helper.getCoinBalance(component, event, helper);
        helper.getInfoUser(component, event. helper);
        helper.setCoinExpiryDate(component, event, helper);
    }
})
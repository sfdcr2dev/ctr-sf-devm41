global class CTRBlockCustomerBatchScheduler implements Schedulable {
    
    global void execute(SchedulableContext sc) {
        // CTRBlockCustomerBatch batch = new CTRBlockCustomerBatch();
        // Database.executeBatch(batch);
    }
}